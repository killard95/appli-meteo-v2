{
  "name": "app-meteo-v2",
  "version": "1.0.0",
  "description": "application meteo pour panneau d'affichage dans les transports en commun a partir d'une API",
  "main": "meteo.js",
  "scripts": {
    "start": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mika",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.5.0",
    "nodemon": "^3.1.10"
  }
}
